language: generic
sudo: required
services:
    - docker
before_install:
    - docker build -t uppalavijay/frountend:latest -f Dockerfile.dev .
script:
    - docker run -e CI=true uppalavijay/frountend npm run test